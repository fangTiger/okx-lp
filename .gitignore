# 虚拟环境与字节码
.venv/
__pycache__/
*.py[cod]

# 运行时数据与日志（含钱包地址，不入库）
log/
*.jsonl

# 例外：校准数据快照（纯链上数据，不含钱包地址），需要跨机器传递
!data/snapshots/*.jsonl

# 密钥与私密配置（绝不入库）
.env
.env.*
!.env.example
secrets/
config/secrets.env
*keystore*.json
UTC--*
*.key

# 本地状态
state/
intents/
.DS_Store
